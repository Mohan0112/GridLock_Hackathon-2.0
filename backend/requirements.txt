# Gridlock backend — full runtime stack
pandas>=2.2
numpy>=1.26
duckdb>=1.1
h3>=4.1
scikit-learn>=1.4          # forecasting (HistGradientBoostingRegressor ensemble)
fastapi>=0.110
uvicorn[standard]>=0.29
apscheduler>=3.10          # nightly recompute seam
pydantic>=2.6
python-multipart>=0.0.9    # /api/ingest file upload
pytest>=8                  # smoke tests
httpx>=0.27                # FastAPI TestClient transport
